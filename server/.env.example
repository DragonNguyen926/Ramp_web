# --- Server env (template) ---
# Postgres connection string (Neon or other). Must include sslmode=require for Neon.
DATABASE_URL= postgresql://neondb_owner:npg_pCDPa4NuHZ2E@ep-morning-firefly-a1w2zt0u-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
# Comma-separated list of allowed origins (no trailing slashes)
# Include your Netlify site and local dev port.
CORS_ORIGIN=http://localhost:5173,https://rampcsub.netlify.app

# Secret used for signing tokens/cookies (set a long random string in real .env)
JWT_SECRET=Emyeuanh9226

# Optional: Render will inject PORT automatically in production
# PORT=5174
